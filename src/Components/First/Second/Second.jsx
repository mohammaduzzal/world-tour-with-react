

const Second = ({country, handleVisitedCountries,handleFlags}) => {
    return (
        <div>
            <h6><small>country : {country.name.common}</small></h6>
        </div>
    );
};

export default Second;